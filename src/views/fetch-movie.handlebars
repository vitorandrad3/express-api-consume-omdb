<form action="/fetch-movie" method="POST">
    <input id="movieNameInput" name="search" placeholder="movie name">
    <button type="submit">Pesquisar</button>
</form>


<div class="list">
    {{#each movie}}
    <div class="item">
        <a href="/movie-details/{{movieId}}">
            <img src="{{posterUrl}}" />
        </a>
        <h2>{{title}}</h2>
    </div>
    {{/each}}
</div>